import styled from 'styled-components'

const mainBG = '#222';
const mainColor = '#fff';
const secondary = '#375a7f';

export const ContainerStyled = styled.div`
	display: flex;
	flex-direction: column;
	justify-content: space-between;
	align-items: center;
	max-width: 1280px;
	margin: 0 auto;
`